{
  "name": "svelte-retag-demos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist/ && vite build && yarn build:iife",
    "build:iife": "VITE_BUILD_FORMAT=iife yarn vite build && cp iife.html dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.4.6",
    "svelte": "^4.0.5",
    "vite": "^4.5.1"
  }
}
