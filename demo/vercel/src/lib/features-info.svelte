<p>View the
	<a href="https://github.com/patricknelson/svelte-retag/blob/main/demo/vercel/index.html" target="_blank" rel="noopener">page
		source 🚀</a>. This demo is implemented entirely using via <code>svelte-retag</code> with all Svelte components
	organized on the page using custom elements (or &ldquo;web components&rdquo;). Featuring:</p>
<ul>
	<li><strong>Slots</strong> (default and named slots)</li>
	<li><strong>Nesting</strong> within slots</li>
	<li><strong>Context</strong> (including nested context, see &ldquo;Examples&rdquo; tab)</li>
	<li><strong>Vite HMR updates</strong> (if launched locally)</li>
	<li>SSR/SSG via <strong>hydration</strong> <em>(experimental)</em></li>
</ul>

<h3>Performance</h3>
<p>This demo also shows the differences between deferred Modules and IIFE, particularly with regard to CLS (Cumulative
	Layout Shift). Use the <strong>Demos</strong> navigation above and compare each one by opening DevTools, disabling
	cache, and then enabling network throttling (e.g. try "Fast 3G").</p>
