<p>All element attributes in <em>this particular</em> demo are configured to be dynamic. Try editing the
	<code>pagetitle</code> in <code>&lt;app-tag pagetitle=&quot;...&quot;&gt;</code> above or any of the
	<code>count</code> or <code>award</code> attributes in any of the <code>&lt;counter-tag&gt;</code> buttons below.</p>
<p>For example: Each counter below takes an initial <code>count</code> attribute with an <code>award</code>
	value, at which point a 🎉 emoji is first displayed (with randomized emojis after that). Press <code>+</code> or <code>-</code>
	keys on your keyboard to quickly increase/decrease the count.</p>
